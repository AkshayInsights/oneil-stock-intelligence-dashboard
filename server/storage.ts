// This dashboard is a static daily intelligence feed (Excel export + AI
// research bundled into the client). No persistent storage/database is
// required, so this file is intentionally a no-op placeholder to satisfy
// the template's server bootstrap.
export interface IStorage {}
export const storage: IStorage = {};
